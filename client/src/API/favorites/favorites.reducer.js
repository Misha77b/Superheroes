import { 

} from "./favorites.actions";

const initState = {
}

const favoritesReducer = (state = initState, action) => {
    switch(action.type) {

        

        default:
            return state
    }
}

export default favoritesReducer